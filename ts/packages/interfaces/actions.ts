interface Actions {
    scroll(): void;

    scrollToView(selector: string): void;

    tap(selector: string): void;

    // tap(location: Location): void;

    // longPress(selector: string): void;

    // longPress(location: Location): void;

}
